# Session Notes — 2026-07-28

## What Was Done This Session (July 28)

### Major Achievements
- **Full crafting simulator complete** — all currencies, essences, omens, desecration, sockets, quality, Vaal Orb, Divine Orb with value rolling
- **Verified against Craft of Exile** — mod weights match EXACTLY (63700 prefix, 76500 suffix for Gloves_dex)
- **Interactive CLI (poe2-craft sim)** — full REPL with save/load (.json + .txt PoB format)
- **153 tests passing**

### Key Fixes Applied During Testing
- Essences: Magic → Rare (not Normal → Magic), only adds 1 guaranteed mod (no fill)
- Alchemy: works on Normal OR Magic → Rare
- Vaal: works on Normal, reroll fills to 6 mods, proper implicits as ModInstance
- Chaos: removes 1 + adds 1 (not full reroll)
- Reforge: requires same rarity, per-rarity stock tracking
- Scour: conceptual reset, no rarity restriction, excluded from cost
- Desecration pool: normal mods + abyss-exclusive (combined)
- Family blocking on essences
- Transmute visible in currencies for Normal items
- Augment blocked when item is full

### CLI Commands Available
currencies, essences, omens, bones, pool, dpool, vpool, socketables,
socket, unsocket, artificer, quality, desecrate, reveal, essence,
export (pob/trade), save/load (.json/.txt), cost, stock, stash, buy_base,
history, help

### What's Next
1. **Continue testing** against Craft of Exile recipes (alt-regal flows, omen combos)
2. **Start optimizer gene.py** — Rule, RuleList, Condition, Action dataclasses
3. Remaining nice-to-haves: Catalysts, Recombinator, Delirium jewel crafting

### Architecture Summary
```
src/poe2_crafting_mcp/crafting/
  simulator.py      — ItemState, ModInstance, CraftingSimulator, CURRENCIES, OMENS
  desecration.py    — DesecrationEngine, bones, reveal pool, probability calc
  essence_resolver.py — maps essence name + slot → stat_text + mod_family
  socketables.py    — query rune/core/idol effects from DB
  pob_export.py     — ItemState ↔ PoB text format (import + export)
  terminal_value.py — post-mod cost/value calculator
```

### Data Status (all seeded, verified)
- mod_weights: 23,210 mods, 60 item classes (normal/essence/desecrated/corrupted/socketable/bonded/soul pools)
- essences: 308 effects from 95 essences (100% stat_text linkage)
- prices: 1063 items from poe.ninja
- Mod weights MATCH Craft of Exile exactly
