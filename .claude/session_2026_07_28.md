# Session Notes — 2026-07-28

## What Was Done This Session

### Research & Data
- Confirmed all 95 essences scraped and 100% stat_text linkage to mod_weights
- Confirmed omen stacking rules (multiple omens combine on one currency use)
- Confirmed Vaal Orb outcomes (4 equally-weighted per poe2wiki)
- Confirmed desecration pool = normal mods + abyss-exclusive (lich) mods combined
- Confirmed weapon classification (Bow = ranged martial, Talisman = 2H melee martial)
- Fixed Talisman classification (was Jewellery, now Martial Weapon)
- Added all Greater/Perfect currency variants to currencies.py with correct descriptions

### Simulator Fixes
- Essences: Magic → Rare (not Normal → Magic), no random fill (just +1 mod)
- Essences: family blocking when family already on item
- Essences: correct tier placement via stat_text pinning
- Alchemy: works on Normal OR Magic → Rare with 4 mods
- Scour: no rarity restriction (conceptual reset)
- Transmute: shows in currencies for Normal items (has to_rarity)
- Augment: blocked when item is full (0 open slots)
- Vaal Orb: works on Normal items, reroll fills to 6 mods, proper implicit model
- Divine Orb: actually rerolls values within tier ranges

### New Features
- Multi-omen stacking (29 omens, `omens=["a", "b"]`)
- Desecration engine (bones, reveal, faction filtering, Abyss Mark)
- Abyss Mark tier upgrade tracking (reveal > removed mod's req_level)
- Sockets & Quality (Artificer's Orb, max per slot, Vaal +1)
- Socketable effects resolved from DB (4971 entries, slot-dependent)
- Actual value rolling + inline display format (+26(25-27), 72%(68-79))
- Vaal implicits as proper ModInstance in implicits list
- Reforging with stock management
- PoB export (item → text format)
- Terminal value function
- Cost tracking with price lookup
- poe2-lookup omens command

### CLI Commands Added
- `currencies` — valid currencies for current state
- `essences [tier]` — available essences for slot
- `omens <currency>` — applicable omens
- `bones` — valid bones
- `pool [prefix|suffix] [--min N]` — normal mod pool
- `dpool [affix] [--omens x]` — desecration reveal pool
- `vpool` — Vaal corruption implicits
- `socketables [filter]` — browse runes/cores/idols with effects
- `socket <name>` — place socketable (shows resolved effect)
- `cost` — total currencies consumed with prices
- `export pob` / `export trade` — item text formats
- `stock` / `stash` / `buy_base` — reforge management
- `essence <name>` — apply by name (auto-resolves)
- `desecrate <bone> [--omens]` — interactive reveal

### Tests
- 150 tests passing

## What's Next

### Immediate (next sessions):
1. **Test against Craft of Exile** — follow known crafting guides step-by-step
   in our sim and verify outcomes match (probabilities, pool sizes, mod weights)
2. **Edge cases** — find and fix remaining mechanics bugs
3. **MCP agent integration testing** — ensure the agent can use tools correctly

### After validation:
4. **Optimizer gene.py** — Rule, RuleList, Condition, Action dataclasses
5. **Optimizer engine** — NSGA-II, fitness evaluation, GP operators

## Known Issues / Areas to Verify
- Alteration: should reroll Magic to 1-2 mods (verify correct behavior)
- Augment on Magic with 1 mod: adds 1 mod (verify affix type logic)
- Regal on Magic: adds 1 mod + upgrades to Rare (verify keeps existing)
- Chaos on Rare: removes 1 + adds 1 (not full reroll — verify)
- Fracturing Orb: requires 4+ mods, fractures random one (verify)
- Recombinator: not modeled yet (complex merge mechanic)
- Catalysts: quality for jewellery (increases weight of tagged mods) — not modeled
