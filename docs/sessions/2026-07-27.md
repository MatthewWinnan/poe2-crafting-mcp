# Session Notes — 2026-07-27

## What Was Done This Session

### Bug Fixes
- Wiki implicit resolution for tablets (Breach Tablet → "Adds an Otherworldy Breach to a Map")
- Rate limiting on wiki client (5 retries, 30s backoff, 3s inter-batch)
- ETL bug: _load_concepts passed sqlite3.Connection instead of path to PriceDatabase
- Fixed probability model: each tier competes independently (not one-per-family)

### New Features — Crafting Advisor (Sprint 5a + 5c)
- **poe2db scraper**: extracts real spawn weights from inline HTML JSON (DropChance field)
- **mod_weights table**: stores all weights per item class/pool/tier
- **get_craftable_mods**: MCP tool + CLI with correct probability calculations
- **Currency filtering**: Greater (min_lv=35-44) / Perfect (min_lv=50-70) narrows pool
- **Dual percentage display**: prefix% and total pool% (like Craft of Exile)
- **craft-cost**: estimate expected cost for hitting a target mod
- **craft-compare**: rank all currencies by cost-efficiency with live prices
- **craft-item**: analyze a found/trade item, identify mod families, show what's available
- **craft-sim**: Monte Carlo simulation (basic, needs perf work)
- **--mod filter**: search within mod pool by text
- **--existing-mods**: factor in mods already on item (dynamic pool blocking)
- **Live prices wired in** from poe.ninja economy cache

### Infrastructure
- `poe2-lookup status` — central DB health view
- `poe2-lookup seed-all` — one command for ETL + concepts + item-desc
- MCP Resources: 4 crafting guides (methods, modifiers, corruption, defences)

### Key Findings
- poe2db DropChance = real spawn weight (community-derived, same source as Craft of Exile)
- All currencies use SAME normal pool, differ only by operation (add/del/del_add/reroll)
- Chaos = del_add (remove 1 uniform, add 1 weighted) — NOT full reroll
- Greater/Perfect filter by min_mod_level, narrowing the pool dramatically
- Economy: Exalted = 0.02c, Divine = 8.79c, Chaos = 1.0c (divine is the real currency)
- Annulments expensive (4.1c), Omens vary widely (0.6c to 134c)

## Current State
- 72 tests passing
- Mod pool seeded for Gloves_int and Amulets (user running full seed)
- Full crafting analysis workflow working end-to-end via CLI
- Trade integration working for buy-vs-craft comparison

## What's Next (Sprint 5e)
1. Performance: Rust inner loop for Monte Carlo (or numpy)
2. Tier identification in craft-item output
3. Crafting strategy research (scrape guides, encode rules)
4. Evolutionary optimizer (batch simulate → evaluate → decide next step)
5. Multi-step recipe simulation with base cost factoring
