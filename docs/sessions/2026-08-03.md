# Session Notes — 2026-08-03

## What Was Done This Session

### Repo Restructure
- Flattened `src/poe2_crafting_mcp/` to top-level
- Organized `.claude/` into `docs/design/`, `docs/research/`, `docs/sessions/`
- Added `crates/poe2-optimizer/` Rust workspace
- Added rust-overlay to Nix flake (Rust 1.97.1 + maturin)
- 153 tests passing after restructure

### Optimizer Module (Complete Python + Rust)

```
poe2_crafting_mcp/crafting/optimizer/
├── gene.py          — 26 predicates, 37 currencies, 12 omens, full crafting domain
├── seeds.py         — 10 heuristic strategies from 0.5 Runes of Aldur meta
├── operators.py     — 1 crossover + 6 credit-guided mutations (BASIL/PPL-ST)
├── nsga2.py         — Balanced NSGA-II (Doerr 2024, fixes 3-objective failure)
├── qd_archive.py    — MAP-Elites 48-cell grid (4×4×3 behavioral descriptors)
├── bridge.py        — Python ↔ Rust serialization + fallback stub
└── runner.py        — Full GP loop, CraftingStrategy + OptimizationResult

crates/poe2-optimizer/src/
├── lib.rs           — PyO3 module entry
├── item_state.rs    — Compact state + flags (desecrated/divined/essence)
├── pool.rs          — Weighted sampling with rejection
├── conditions.rs    — All 26 predicates
├── actions.rs       — All 37 currencies + omens
├── evaluate.rs      — MC loop + per-rule credit (fire_on_success/failure)
└── batch.rs         — Rayon parallel + numpy I/O (evaluate_population)
```

### Research Applied
- **Representation:** 7 alternatives evaluated, rule-list confirmed (PPL-ST, BASIL)
- **Operators:** Simplified from 10 to 6 types (BASIL shows QD handles diversity)
- **NSGA-II:** Balanced tie-breaking (Doerr 2024) fixes exponential failure on 3 objectives
- **QD Archive:** MAP-Elites with noise-resilient replacement margin
- **Crafting meta:** Full 0.5 Runes of Aldur patterns (essence+desecrate+exalt, fracture, whittling)

### Key Design Decisions
- Rust inner loop from day one (no Python prototype)
- Divine-before-fracture as a mid-strategy action (not terminal)
- Abyss/desecration as mod-strategy (slot reservation via rule priority)
- Vaal as agent-level decision (not optimizer-level)
- Multiple starting states (blank, magic_with_target, fractured_target)
- Cost breakdown: base + currency + total vs trade

### Test Count: 216 passing

## What's Next (Next Session TODO)

### 1. Add Rune/Faction Pools to Optimizer
- [ ] Encode decay/marksman/destruction/chronomancy/berserking pools in preflight.py
- [ ] Add pool parameter to ESSENCE_UPGRADE action (which pool to draw from)
- [ ] Rust: `add_specific_mod` supports selecting from faction-specific weight arrays
- [ ] CLI/MCP: `--pool decay` or auto-detect from item_class + target family
- [ ] Test: Marksman (projectile damage) on Gloves_int, Decay (ailment) on Gloves

### 2. Fix Optimizer Shortcomings
- [ ] **Sub-goal decomposition** (Path A): `optimize_multi_target()` that runs
      optimizer for each target step independently, sums costs
- [ ] **Adaptive trial count** (Path E): start with 200 trials, increase to 5000
      for top 10% performers in later generations
- [ ] **Recipe mode** (Path C): `evaluate_recipe(steps, pool, prices)` that
      costs out a user-specified crafting sequence without GP evolution

### 3. Simulation Fidelity Upgrades
- [ ] Essence pool (not normal pool) for guaranteed mod tier selection
- [ ] Greater Exaltation omen: add 2 mods in one exalt (model in Rust)
- [ ] Omen of Light: annul only desecrated mod (safe desecration retry)
- [ ] Catalysing Exaltation: bias exalt toward catalyst tag family
- [ ] Regal behavior: model restart-before-essence if regal adds bad prefix
- [ ] Perfect Essence (swap): remove + add from perfect_essence pool

### 4. Integration Cleanup
- [ ] Maturin .whl build integrated into Nix flake (not just symlink)
- [ ] preflight.py: auto-merge desecrated pool when targets include abyss families
- [ ] preflight.py: auto-detect rune pool from target families
- [ ] CLI: accept item_class aliases ("int gloves" → "Gloves_int")
- [ ] MCP: wire up sub-goal decomposition as default for >3 targets
